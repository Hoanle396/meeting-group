{
  "name": "teams",
  "version": "1.0.0",
  "description": "A free WebRTC browser-based video call",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "serve": "lt --port 3000 --subdomain dacs-meet",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "localtunnel": "^2.0.2",
    "ngrok": "^4.0.1",
    "socket.io": "^4.1.2"
  }
}
